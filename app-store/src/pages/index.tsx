import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className={`flex items-center justify-center w-full h-full`}>
      {/* <Image alt="Logo" src="/logo.png" width={730} height={500} /> */}
    </div>
  );
};

export default Home;
